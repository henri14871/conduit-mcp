{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter server dev",
    "lint": "pnpm -r lint",
    "test": "pnpm --filter server test"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "prettier": "^3.4.0"
  }
}
